import React from "react";
import { useParams } from "react-router-dom";

const GroupFormation = () => {
  const { id } = useParams();
  return <div>GroupFormation </div>;
};

export default GroupFormation;
